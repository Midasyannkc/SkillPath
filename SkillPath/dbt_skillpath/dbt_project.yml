name: 'skillpath'
version: '1.0.0'
config-version: 2

profile: 'skillpath'

model-paths: ["models"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  skillpath:
    staging:
      +materialized: view
    marts:
      +materialized: table
